# ============================================================
# Status Orbit Menubar — Frozen Seed v2 (evolved from v1)
# ============================================================
# Parent: seed_orbit_v1
# Trigger: Railway 가 공개 statuspage v2 endpoint 미제공 (자체 SPA-only).
#          User consensus 로 v1 의 11 services 를 10 services 로 축소 결정.
# ============================================================

parent_seed_id: seed_orbit_v1
lineage_id: lineage_orbit
generation: 2
created_at: 2026-05-17

# === 변경 사항 (v1 대비) ===
changes:
  - kind: ontology_drift
    field: monitored_services_count
    from: 11
    to: 10
  - kind: service_removed
    id: railway
    reason: |
      status.railway.com 은 statuspage v2 표준이 아닌 자체 SPA + admin API (auth 필요).
      공개 endpoint 부재로 v1 의 statuspageV2 어댑터로 fetch 불가.
      Future work: Railway 전용 provider (HTML scraping 또는 별도 API 발견) 작성 후 v3 에서 재포함.

# === v2 의 합의 ===
goal: |
  v1 goal 유지, 단 services 카운트 11 → 10 (AI 5 + Infra 5: GitHub, Cloudflare,
  Vercel, Supabase, Netlify) 로 조정.

# v1 의 나머지 모든 필드 (constraints, acceptance_criteria 의 measurable 수치 등)
# 는 services count 의존 항목을 자동으로 10 으로 읽어 재해석한다.
# 명시적 재기록은 생략 (frozen 원칙 유지하면서 lineage 로 변경 추적).

metadata:
  seed_id: seed_orbit_v2
  ambiguity_score: 0.10   # v1 의 0.1525 보다 더 낮음 (Railway 모호성 해소)
  ambiguity_gate_passed: true
  authored_by: joon (with claude-opus-4-7)
  notes: |
    Ouroboros 패턴에서는 이런 "ontology drift" 가 consensus trigger.
    실제 Ouroboros 라면 3 모델 투표가 발동하지만, 본 mini-Ouroboros 에서는
    사용자 단독 consensus 로 통과.
